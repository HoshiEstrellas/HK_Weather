<view class="page">
  <view class="header">
    <text class="eyebrow">HISTORY</text>
    <text class="title">历史获取数据</text>
    <text class="subtitle">当前 10 分钟区间之前 1 小时内获取到的天气记录</text>
  </view>

  <view class="search">
    <input value="{{keyword}}" bindinput="onKeywordInput" confirm-type="search" bindconfirm="onSearch" placeholder="搜索灯柱编号，如 GF3637" />
    <button bindtap="onSearch">搜索</button>
  </view>

  <view class="error" wx:if="{{error}}">{{error}}</view>
  <view class="loading" wx:if="{{loading}}">加载中...</view>

  <view class="list" wx:else>
    <view
      class="weather-row"
      wx:for="{{history}}"
      wx:key="id"
      bindtap="openDetail"
      data-lp="{{item.lpNumber}}"
      data-device="{{item.deviceId}}"
    >
      <view class="row-main">
        <view>
          <text class="lp mono">{{item.lpNumber}}</text>
          <text class="device">设备 {{item.deviceId}}</text>
        </view>
        <text class="temp">{{item.temperatureText}}</text>
      </view>
      <view class="row-data">
        <text>湿度 {{item.humidityText}}</text>
        <text>风速 {{item.windSpeedText}}</text>
        <text>风向 {{item.windDirectionText}}</text>
      </view>
      <view class="row-foot">
        <text>获取 {{item.fetchedText}}</text>
        <text>观测 {{item.observedText}}</text>
      </view>
    </view>
    <view class="empty" wx:if="{{history.length == 0 && !error}}">
      暂无前一小时历史获取记录。
    </view>
  </view>
</view>
